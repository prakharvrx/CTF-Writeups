Connect using SSH

After Connect, using `cd` command to go to the directory `/inhere` and if you can use the command like `ls -la` then you will see the directory name which is start from `..` this sign can be used for the parent directory, so using that command `ls ...Hiding-From-You` so you get into that directory and using command like `cat` to the content of that file `Hiding-From-You`

password : `2WmrDFRmJIq3IPxneAaMGhap0pFhF3NJ`

